# Supabase (módulo Tracker + auth + persistência)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Meta Graph API (Monitor + Uploader + Pixel) — token colado em runtime, não aqui
# Mantido só para referência de versão da API
VITE_META_API_VERSION=v22.0

# Tracker Ads (Ad Intelligence) — URL do scraper-service pro botão "Auto" (cloud/Railway).
# O botão "Local" usa http://localhost:3001 por padrão (scraper rodando na sua máquina).
VITE_SCRAPER_URL=
